subject program includ edg detect algorithm thi program doesn t detect edg with compass oper and a laplacian oper it should output raw grei scale imag with edg the output doesn t look like edg at all in novice term how do i correct the error ani improv ar welcom i ll even accept your correct code if i convolv the input imag with a digit gaussian by to remov nois will i get an improv with the laplacian type of edg detect includ stdio h includ math h defin imagewidth defin imageheight unsign char input imag imageheight imagewidth unsign char angl want imageheight imagewidth unsign char magnitud imag imageheight imagewidth int laplac op int compass op int compass op int compass op int compass op int compass op int compass op int compass op int compass op void compass row col int row col int valu int op row op col int compass compass compass compass int compass compass compass compass compass compass compass compass compass compass compass compass for op row op row op row for op col op col op col if row op row col op col compass int input imag row op row col op col compass op op row op col compass int input imag row op row col op col compass op op row op col compass int input imag row op row col op col compass op op row op col compass int input imag row op row col op col compass op op row op col compass int input imag row op row col op col compass op op row op col compass int input imag row op row col op col compass op op row op col compass int input imag row op row col op col compass op op row op col compass int input imag row op row col op col compass op op row op col if compass compass valu compass els valu compass if valu compass valu compass if valu compass valu compass if valu compass valu compass if valu compass valu compass if valu compass valu compass if valu compass valu compass magnitud imag row col char valu void laplac row col int row col int op row op col magnitud imag row col for op row op row op row for op col op col op col if row op row col op col magnitud imag row col char int magnitud imag row col int input imag row op row col op col laplac op op row op col main file origin imag fp file laplac mag fp laplac mag fp laplac mag fp file compass mag fp int row col algo count origin imag fp fopen input imag rb laplac mag fp fopen laplac magnitud wb compass mag fp fopen compass magnitud wb fread unsign char input imag sizeof unsign char imageheight imagewidth origin imag fp for algo count algo count algo count for row row imageheight row for col col imagewidth col if algo count laplac row col els compass row col if algo count fwrite magnitud imag sizeof char imageheight imagewidth laplac mag fp els fwrite magnitud imag sizeof char imageheight imagewidth compass mag fp for a lot of au music ftp sound sdsu edu then cat file au dev audio ub librari catalog telnet bison acsu buffalo edu when in doubt ask xarchi xgopher or xwai 